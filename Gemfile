# frozen_string_literal: true

source 'https://rubygems.org'

gem 'json'
gem 'rspec'
gem 'sinatra', require: 'sinatra/base'
gem 'sinatra-contrib' # enables file uploads

group :development, :test do
  gem 'rubocop', require: false
end

group :test do
  gem 'rack-test'
end
